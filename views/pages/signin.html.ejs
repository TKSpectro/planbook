<div class="text-center container" style="max-width: 30%">
    <h1 class="h3 mb-3 font-weight-normal text-primary">Login</h1>
    <form id="signinForm" onsubmit="return loginPressed(event)">
        <label for="email" class="sr-only">Email</label>
        <input
            type="email"
            id="email"
            class="form-control"
            placeholder="Email"
            required
            autofocus
        />

        <label for="password" class="sr-only">Password</label>
        <input
            type="password"
            id="password"
            class="form-control"
            placeholder="Password"
            required
        />

        <button class="btn btn-lg btn-primary btn-block mt-4" type="submit">
            Sign In
        </button>
    </form>
</div>
