<div
    class="modal fade"
    id="leaveOwnedHouseholdModal"
    tabindex="-1"
    aria-labelledby="leaveOwnedHouseholdModalLabel"
    aria-hidden="true"
>
    <div class="modal-dialog">
        <div class="modal-content bg-dark">
            <div class="modal-header">
                <h5 class="modal-title" id="leaveOwnedHouseholdModalLabel">
                    Please select a new owner for the household
                </h5>
                <button
                    type="button"
                    class="close"
                    data-dismiss="modal"
                    aria-label="Close"
                >
                    <span aria-hidden="true" class="text-white">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form
                    id="leaveOwnedHouseholdModalForm"
                    onsubmit="return leaveOwnedHousehold(event)"
                >
                    <div class="d-none">
                        <input id="editPaymentId" type="text" />
                    </div>
                    <div class="form-group row">
                        <label
                            for="chooseNewOwnerSelect"
                            class="col-sm-3 col-form-label"
                            >New owner
                        </label>
                        <div class="col-sm-9">
                            <select
                                class="form-control selectpicker"
                                id="chooseNewOwnerSelect"
                                data-live-search="true"
                                required
                            ></select>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="submit" class="btn btn-danger">
                            Leave
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
